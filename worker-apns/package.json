{
  "name": "kypost-push-relay-apns",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that delivers KyPost native push notifications to iOS devices via Apple Push Notification service (APNs).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.112.0"
  }
}
